# third_party_blind_process_validation_set

该目录为按第三方盲测标签模板构造的流程验证样本集，用于预先检查样本接入、标签读取、指标计算和报告生成链路。

重要说明：

1. 本目录不是独立第三方正式盲测结果。
2. 不应将本目录中的指标写作真实第三方提供未知样本后的结论。
3. 正式第三方结果仍应使用 `test_data/third_party_blind_set/third_party_labels_filled.csv` 触发。
4. 本目录可用于提前检查标签模板、脚本兼容性、指标口径和报告生成流程。
